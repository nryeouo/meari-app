# プログラム起動から曲再生終了までの動作

この文書は、`meari-app` のプログラムが起動してから、1曲の再生が終了するまでの流れをまとめたものです。
ChatGPT Codex によって 2026-04-30 に作成されました。

## 1. 起動時の初期化

`docker/app.py` を起動すると、最初に次の初期化が行われます。

- `aboutApp` からアプリ名・版情報・所有者情報を読み込みます。
- Flask アプリ本体を作成します。
- Firebase Admin SDK の初期化を行い、Firestore クライアントを作成します。
- Google Cloud Storage クライアントを作成し、`meari-video` バケットを扱えるようにします。
- CORS を有効化し、Blueprint を登録します。

最後に `app.run(host="0.0.0.0", port=8080, debug=True)` で Web サーバー待受状態になります。

## 2. 利用者が画面を開く

利用者がブラウザで `/` にアクセスすると、`static/index.html` が返され、画面操作が可能になります。

- `/about` で版情報を取得できます。
- `/event-info` で現在時刻（UTC）に有効なイベント情報を取得します。

## 3. 次に再生する予約曲の決定

画面側は `/next_reserved_song` を呼び出し、次に再生する候補曲を受け取ります。

このときサーバー側では次の処理を行います。

1. Firestore `reservations` から、基本は `order` → `created_at` 順で最大20件を取得します。
2. 取得失敗時は `created_at` 順で再試行します。
3. `canceled` / `finished` / `playing` など、再生対象外の `status` は除外します。
4. 残った曲について、`id`・`songNumber`・`order`・`created_at` などを整形して返します。

## 4. 曲情報の取得

曲番号が決まると、`/song_info/<songNumber>` を呼び出して曲の詳細を取得します。

- `songlist.sqlite` に接続し、`songs` テーブルから `songNumber` 一致行を検索します。
- 一致データを JSON として返します。

## 5. 再生素材（動画・BGM）の取得

### 動画

`/video/<filename>` を呼び出すと、対象ファイルが存在する場合は 15 分有効の署名付き URL を返します。

- ファイル不存在の場合は `404` を返します。

### BGM（必要時）

- `/bgm_list` で `bgm/` 配下の `*.mp3` 一覧を取得できます。
- `/bgm/<filename>` では、BGM ファイルの署名付き URL へリダイレクトします。

## 6. 再生中

再生自体は、取得したメディア URL を使ってクライアント（ブラウザ側）で行われます。

- サーバーは主に「予約情報」「曲情報」「再生用 URL」を供給します。
- 曲が終わると、クライアントは次の予約曲の取得（`/next_reserved_song`）へ進み、次曲処理に移ります。

## 7. 曲再生終了時点のまとめ

1曲の再生終了までに、サーバー側で重要なのは以下の責務です。

- 次曲候補の抽出（Firestore）
- 曲メタ情報の参照（SQLite）
- 再生用実ファイルへの一時アクセス手段の発行（Cloud Storage 署名付き URL）

以上を組み合わせることで、起動後から曲再生終了までの一連の流れが成立します。
